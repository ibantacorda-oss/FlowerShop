name: popo

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cloud_firestore: ^6.3.0
  firebase_auth: ^6.4.0
  firebase_data_connect: ^0.3.0
  firebase_storage: ^13.3.0
  flutter:
    sdk: flutter
  path: ^1.9.1
  sqflite: ^2.4.2

flutter:
  uses-material-design: true

  assets:
    - lib/assets/images/